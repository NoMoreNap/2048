import {createTheme} from '@mui/material';

export  const theme = createTheme({
    palette: {
        primary: {
            main: '#7480F3',
            contrastText: 'black'
        },
        secondary: {
            main: '#fff',
            contrastText: 'white'
        }
    },
    components: {
    },
})
